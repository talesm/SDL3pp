--- build/generated/SDL3pp_metal.h
+++ include/SDL3pp/SDL3pp_metal.h
@@ -7,7 +7,7 @@
 namespace SDL {
 
 /**
- * @defgroup CategoryMetal Category Metal
+ * @defgroup CategoryMetal Metal Support
  *
  * Functions to creating Metal layers and views on SDL windows.
  *
@@ -55,7 +55,7 @@
    *
    * @sa Metal_CreateView
    */
-  void Metal_DestroyView();
+  void Destroy();
 
   /**
    * Get a pointer to the backing CAMetalLayer for the given view.
@@ -181,12 +181,12 @@
  *
  * @sa Metal_CreateView
  */
-inline void Metal_DestroyView(MetalViewRef view, MetalViewRef view)
+inline void Metal_DestroyView(MetalViewRaw view)
 {
-  Metal_DestroyView(release());
+  SDL_Metal_DestroyView(view);
 }
 
-inline void MetalViewBase::Metal_DestroyView() { Metal_DestroyView(release()); }
+inline void MetalViewBase::Destroy() { Metal_DestroyView(release()); }
 
 /**
  * Get a pointer to the backing CAMetalLayer for the given view.
